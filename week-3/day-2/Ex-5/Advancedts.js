function getLength(value) {
    return value.length;
}
var stringLength = getLength('Hello,Sahil');
console.log(stringLength);
var arrayLength = getLength([1, 2, 3, 4, 5, 'sahil']);
console.log(arrayLength);
